# 2026-07-27 Daily Log

## Bot 啟動排錯（ai-bot）

**目標**：啟動 bot 並提供 TG 手動測試清單（基本指令 + agent 對話 + kiro-cli 驅動）

### 修復的三個錯誤

#### 1. `create_app(registry=registry)` 參數不符
- **檔案**：`src/server/main.py` lifespan 中傳了 `registry=registry`
- **問題**：`src/bot/main.py` 的 `create_app()` 沒有 `registry` 參數 → TypeError
- **修法**：移除多餘的 `registry=registry` 關鍵字引數

#### 2. `wiki_engine.build_index()` 方法不存在
- **檔案**：`src/server/main.py` lifespan 呼叫
- **問題**：`WikiEngine` 只有 `ingest` / `query` / `lint`，沒有 `build_index`
- **修法**：直接刪除 `wiki_engine.build_index()` 這行（engine 是 lazy 初始化，不需要預先 build）

#### 3. `from src.llm import gemini_chat, openai_chat` 找不到
- **檔案**：`src/skills/internal/router.py`
- **問題**：`src/llm/__init__.py` 是空的，不 export 這兩個 module
- **修法**：重寫 router.py，改用 `src.llm.chat.simple_chat`（現有 Provider 層）

### 其他修復

#### 4. `from src.workflow.engine import WorkflowEngine` 路徑錯誤
- **檔案**：`src/skills/internal/schedule_engine.py`
- **問題**：`src/workflow/` 目錄不存在
- **修法**：改為 `from src.skills.internal.workflow_engine import WorkflowEngine`

#### 5. 缺少 optional deps
- `beautifulsoup4` → `news_scraper` 用到
- `apscheduler` → `schedule_engine` 用到
- **修法**：`pip install beautifulsoup4 apscheduler`

### 最終啟動結果
- Skills loaded: 15 ✅
- TG Bot @ark_dev_bot polling ✅
- Port 8000 衝突（有殘留 process 15964）→ Kill 後重啟解決
- Bot 連線驗證：`/getMe` OK，`bot: @ark_dev_bot 已連線`

### 待測試（手動測試清單見下方）

## Bot 指令修復與 Agent 面板優化

- `/status` + `/help` MarkdownV2 bug 修復 → 改用 Markdown（`gemini-3.5-flash` 的 `-` `.` 造成解析失敗）
- `/assign` + `/board` 補上 CommandHandler 註冊（handler 定義了但沒掛到 app）
- TG `/agents` 面板重構：
  - 切換後面板常駐不消失（`edit_message_text` 原地刷新）
  - ✅ 標示即時跟著切換的 agent 移動
  - CLI 不可用改用 `show_alert=True` 彈出提示，不破壞面板
  - 重複點同一 agent 靜默忽略（TelegramError catch）
  - 新增 `_build_agents_keyboard()` + `_build_agents_text()` 輔助函數

## 10:45 [_default] msg-896
- Q: 你是誰
- A: 我是 Ark Agent，你的通用 AI 助手！🚀 

我能幫你解答技術問題、查詢知識庫與歷史記憶，並在需要時將任務派發給專業的 Agent 夥伴。有什麼我可以幫忙的嗎？😊

## 10:46 [_default] msg-898
- Q: 你用哪個模型在幫我做事  你可以寫程式嗎
- A: 我是基於 **Gemini API**（通常為 Gemini 1.5 Pro 或 Flash 系列模型）運作的 AI 助手！🤖✨

**我可以寫程式！** 💻
無論是 Python、JavaScript、API 串接還是演算法，我都能直接幫你撰寫、除錯與優化。

*   **簡單的程式需求**：我可以直接在對話中寫給你。
*   **複雜的專案實作**：我會將任務派發給我們團隊中專門的 **`co

