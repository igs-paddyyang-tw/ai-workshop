# 環境檢查清單

## 課程 A
```bash
python3 --version     # 3.12+
git --version
```
需要：Telegram Bot Token + Gemini API Key

## 課程 B（額外）
```bash
kiro-cli --version    # 2.7+
node --version        # 20+（Web Dashboard）
```

缺少工具時，在 Kiro 聊天框輸入：`檢查我的開發環境`
